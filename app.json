{
  "name": "Basic Redis Rate Limiting Demo Ruby",
  "description": "Blocking requests with Redis on Ruby in Rails",
  "stack": "heroku-18",
  "repository": "https://github.com/redis-developer/basic-redis-rate-limiting-demo-ruby",
  "logo": "https://redis.io/images/redis-white.png",
  "keywords": ["ruby", "ruby-on-rails", "redis"],
  "env": {
    "REDIS_URL": {
      "description": "Redis server url",
      "required": true
    },
    "DATABASE_URL": {
      "description": "Postgres database",
      "required": true
    }
  }
}
